{
  "name": "facis-ai-insight-ui-orce-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Flow-JSON guard tests for the AI Insight UI ORCE flows (node:test).",
  "scripts": {
    "test": "node --test flows/*.spec.js"
  }
}
