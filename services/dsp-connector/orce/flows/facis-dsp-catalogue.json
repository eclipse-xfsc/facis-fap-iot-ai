[
  {
    "id": "tab-dsp-catalogue",
    "type": "tab",
    "label": "FACIS DSP \u2014 Catalogue",
    "disabled": false,
    "info": "FR-DSP-001 \u2014 POST /dsp/catalogue/request.\n\nDSP 2025-1 binding (NF-7, Gap 5): the alias POST /dsp/catalog/request returns a\ndcat:Catalog of dcat:Dataset entries (proper @context/@type/@id, dcat:distribution\n+ odrl:hasPolicy); the FACIS spelling POST /dsp/catalogue/request keeps its native\n{ datasets, nextCursor } shape. Both share the query/mode/iam nodes; dsp-cat-query\npicks the response shape from the request path. See tck/SCOPE-RULING.md.\n\nThe catalogue is DERIVED from the FACIS Data Sink's queryable store: the\nderivation node lists live gold-layer tables in the Trino/Iceberg lakehouse\nand merges them with /data/dsp-config/datasets.json, which acts as a rich\nmetadata OVERLAY and as an offline FALLBACK. See\ndocs/architecture/fap-role-mapping.md for the FACIS Data Sink role.\n\nMerge semantics (dsp-cat-derive-fn):\n  - overlay entry whose gold table is live   -> kept as-is (rich metadata wins)\n  - overlay entry whose table is NOT live    -> dropped (warn)\n  - live gold table with no overlay entry    -> minimal auto-derived entry\n  - catalogueSource = 'lakehouse' on success\n\nAvailability over strictness: on ANY Trino failure the derivation never\nempties an already-populated catalogue; if the catalogue is still empty it\nfalls back to the full overlay file (catalogueSource = 'overlay-fallback').\nThe startup file-read (dsp-cat-read) seeds the catalogue within ms at boot;\nthe derivation replaces it once Trino answers and re-runs every 10 minutes.\n\nFilter + cursor logic mirrors src/dsp-connector/catalogue_store.py:7-139:\n  - filter.assetType        -> exact match on metadata.assetType\n  - filter.datasetIds        -> set membership on dataset.id\n  - page.limit (default 50)  -> slice size\n  - page.cursor              -> integer offset; non-numeric -> 0\n  - nextCursor               -> str(start+limit) when more remain, else null"
  },
  {
    "id": "dsp-cat-bootstrap",
    "type": "inject",
    "z": "tab-dsp-catalogue",
    "name": "@startup",
    "props": [
      {
        "p": "payload"
      }
    ],
    "repeat": "",
    "crontab": "",
    "once": true,
    "onceDelay": "0.5",
    "topic": "",
    "payload": "",
    "payloadType": "str",
    "x": 130,
    "y": 80,
    "wires": [
      [
        "dsp-cat-read"
      ]
    ]
  },
  {
    "id": "dsp-cat-read",
    "type": "file in",
    "z": "tab-dsp-catalogue",
    "name": "read datasets.json",
    "filename": "/data/dsp-config/datasets.json",
    "filenameType": "str",
    "format": "utf8",
    "chunk": false,
    "sendError": true,
    "encoding": "none",
    "allProps": false,
    "x": 360,
    "y": 80,
    "wires": [
      [
        "dsp-cat-load"
      ]
    ]
  },
  {
    "id": "dsp-cat-load",
    "type": "function",
    "z": "tab-dsp-catalogue",
    "name": "load FACIS_DATASETS",
    "func": "let datasets = [];\ntry {\n    if (typeof msg.payload === 'string' && msg.payload.length > 0) {\n        datasets = JSON.parse(msg.payload);\n    } else if (Array.isArray(msg.payload)) {\n        datasets = msg.payload;\n    }\n} catch (err) {\n    node.error('Failed to parse /data/dsp-config/datasets.json: ' + err.message);\n    datasets = [];\n}\nif (!Array.isArray(datasets)) {\n    node.error('datasets.json did not parse to a JSON array; got ' + typeof datasets);\n    datasets = [];\n}\nflow.set('catalogue', datasets);\nnode.log('DSP catalogue: ' + datasets.length + ' dataset(s) loaded');\nreturn null;\n",
    "outputs": 0,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 600,
    "y": 80,
    "wires": []
  },
  {
    "id": "dsp-cat-read-catch",
    "type": "catch",
    "z": "tab-dsp-catalogue",
    "name": "datasets.json missing",
    "scope": [
      "dsp-cat-read"
    ],
    "uncaught": false,
    "x": 360,
    "y": 140,
    "wires": [
      [
        "dsp-cat-init-empty"
      ]
    ]
  },
  {
    "id": "dsp-cat-init-empty",
    "type": "function",
    "z": "tab-dsp-catalogue",
    "name": "init empty catalogue",
    "func": "flow.set('catalogue', []);\nnode.warn('DSP catalogue: /data/dsp-config/datasets.json missing \u2014 serving empty catalogue. Mount the ConfigMap.');\nreturn null;\n",
    "outputs": 0,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 610,
    "y": 140,
    "wires": []
  },
  {
    "id": "dsp-cat-derive-inject",
    "type": "inject",
    "z": "tab-dsp-catalogue",
    "name": "@startup + every 10m",
    "props": [
      {
        "p": "payload"
      }
    ],
    "repeat": "600",
    "crontab": "",
    "once": true,
    "onceDelay": "5",
    "topic": "",
    "payload": "",
    "payloadType": "str",
    "x": 150,
    "y": 200,
    "wires": [
      [
        "dsp-cat-derive-fn"
      ]
    ]
  },
  {
    "id": "dsp-cat-derive-fn",
    "type": "function",
    "z": "tab-dsp-catalogue",
    "name": "derive catalogue from lakehouse",
    "func": "// Derives the catalogue from the FACIS Data Sink's lakehouse: lists live\n// gold-layer tables in Trino and merges them with datasets.json (the rich\n// metadata overlay + offline fallback). Runs at startup and every 10 min.\n// Availability over strictness \u2014 see this tab's info panel for the rules.\nconst catalog = env.get('DSP_TRINO_CATALOG') || 'fap-iotai-stackable';\nconst trinoUrl = env.get('DSP_TRINO_URL') || 'https://trino-coordinator.stackable.svc.cluster.local:8443';\nconst user = env.get('DSP_TRINO_USER') || 'admin';\nconst pass = env.get('DSP_TRINO_PASSWORD') || '';\nconst schema = 'information_schema';\nconst auth = 'Basic ' + Buffer.from(user + ':' + pass).toString('base64');\nconst sql = 'SELECT table_schema, table_name FROM \"' + catalog + '\".information_schema.tables WHERE table_schema IN (\\'gold\\', \\'bronze\\')';\n\n// Read the overlay first (same fs.readFileSync + DSP_DATASETS_PATH pattern as\n// dsp-data-lookup-fn) so it is available both as the merge source and as the\n// Trino-down fallback.\nlet overlay = [];\ntry {\n    const parsed = JSON.parse(fs.readFileSync(env.get('DSP_DATASETS_PATH') || '/data/dsp-config/datasets.json', 'utf8'));\n    if (Array.isArray(parsed)) overlay = parsed;\n} catch (err) {\n    node.warn('catalogue: could not read overlay datasets.json: ' + (err && err.message ? err.message : err));\n}\n\nfunction done(source) {\n    // Unwired completion signal (this node has no downstream in production);\n    // lets the flow-test harness observe termination without polling.\n    node.send({ payload: { catalogueSource: source, size: (flow.get('catalogue') || []).length } });\n}\n\n(async () => {\n    // trinoRequest: copied verbatim from dsp-data-trino-fn (facis-dsp-data.json)\n    // \u2014 same nextUri-chain statement API, auth headers, and rejectUnauthorized.\n    function trinoRequest(urlStr, body) {\n        return new Promise((resolve, reject) => {\n            const u = new urlmod.URL(urlStr);\n            const mod = u.protocol === 'http:' ? http : https;\n            const opts = {\n                method: body ? 'POST' : 'GET',\n                hostname: u.hostname,\n                port: u.port || (u.protocol === 'http:' ? 80 : 443),\n                path: u.pathname + u.search,\n                headers: {\n                    Authorization: auth,\n                    'X-Trino-User': user,\n                    'X-Trino-Catalog': catalog,\n                    'X-Trino-Schema': schema,\n                    'Content-Type': 'text/plain'\n                },\n                rejectUnauthorized: false\n            };\n            const req = mod.request(opts, (res) => {\n                let data = '';\n                res.on('data', (c) => { data += c; });\n                res.on('end', () => {\n                    try { resolve(JSON.parse(data)); } catch (e) { reject(e); }\n                });\n            });\n            req.on('error', reject);\n            if (body) req.write(body);\n            req.end();\n        });\n    }\n\n    try {\n        let result = await trinoRequest(trinoUrl + '/v1/statement', sql);\n        let rows = [];\n        let iterations = 0;\n        while (result && iterations < 25) {\n            iterations++;\n            if (result.error) throw new Error('Trino error: ' + JSON.stringify(result.error));\n            if (result.data) rows = rows.concat(result.data);\n            if (!result.nextUri) break;\n            result = await trinoRequest(result.nextUri, null);\n        }\n\n        // rows are [table_schema, table_name] across gold and bronze.\n        const liveTables = new Set();\n        rows.forEach((r) => { if (r && r[0] && r[1]) liveTables.add(r[0] + '.' + r[1]); });\n\n        const merged = [];\n        const claimed = new Set();\n        overlay.forEach((entry) => {\n            const md = (entry && entry.metadata) || {};\n            const key = md.schema + '.' + md.table;\n            if (liveTables.has(key)) {\n                merged.push(entry);\n                claimed.add(key);\n            } else {\n                node.warn('catalogue: dropping ' + (entry && entry.id) + ' \u2014 table ' + md.schema + '.' + md.table + ' not present in lakehouse');\n            }\n        });\n\n        let autoderived = 0;\n        rows.forEach((r) => {\n            if (!r || !r[0] || !r[1]) return;\n            const key = r[0] + '.' + r[1];\n            if (claimed.has(key)) return;\n            // Auto-derive gold tables only: bronze tables are raw landing\n            // zones and surface in the catalogue solely through curated\n            // overlay entries validated against the live table list above.\n            if (r[0] !== 'gold') return;\n            const tableName = r[1];\n            merged.push({\n                id: 'dataset:facis:' + tableName.replace(/_/g, '-'),\n                metadata: { title: tableName, description: 'Gold-layer dataset ' + tableName + ' (derived from lakehouse metadata)', assetType: 'iot.timeseries', format: 'iceberg/parquet', schema: 'gold', table: tableName, timeColumn: null },\n                offers: [ { id: 'offer:facis:' + tableName.replace(/_/g, '-') + ':read', policySummary: { purpose: 'analytics', rate_limit: '10 req/min', ttl: 'PT1H' } } ]\n            });\n            autoderived++;\n        });\n\n        flow.set('catalogue', merged);\n        flow.set('catalogueSource', 'lakehouse');\n        node.log('DSP catalogue: derived ' + merged.length + ' dataset(s) from lakehouse (' + autoderived + ' auto-derived)');\n        done('lakehouse');\n    } catch (err) {\n        const reason = err && err.message ? err.message : String(err);\n        const existing = flow.get('catalogue');\n        if (Array.isArray(existing) && existing.length > 0) {\n            node.warn('catalogue: Trino derivation failed, keeping existing catalogue (' + existing.length + ' entries) \u2014 ' + reason);\n            done(flow.get('catalogueSource') || null);\n        } else {\n            flow.set('catalogue', overlay);\n            flow.set('catalogueSource', 'overlay-fallback');\n            node.warn('catalogue: Trino derivation failed and catalogue empty, falling back to overlay file (' + overlay.length + ' entries) \u2014 ' + reason);\n            done('overlay-fallback');\n        }\n    }\n})();\nreturn null;\n",
    "outputs": 1,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [
      {
        "var": "http",
        "module": "http"
      },
      {
        "var": "https",
        "module": "https"
      },
      {
        "var": "urlmod",
        "module": "url"
      },
      {
        "var": "fs",
        "module": "fs"
      }
    ],
    "x": 430,
    "y": 200,
    "wires": [
      []
    ]
  },
  {
    "id": "dsp-cat-in",
    "type": "http in",
    "z": "tab-dsp-catalogue",
    "name": "POST /dsp/catalogue/request",
    "url": "/dsp/catalogue/request",
    "method": "post",
    "upload": false,
    "swaggerDoc": "",
    "x": 190,
    "y": 260,
    "wires": [
      [
        "dsp-cat-mode-check"
      ]
    ]
  },
  {
    "id": "dsp-cat-in-alias",
    "type": "http in",
    "z": "tab-dsp-catalogue",
    "name": "POST /dsp/catalog/request (DSP binding spelling)",
    "url": "/dsp/catalog/request",
    "method": "post",
    "upload": false,
    "swaggerDoc": "",
    "x": 190,
    "y": 300,
    "wires": [
      [
        "dsp-cat-mode-check"
      ]
    ]
  },
  {
    "id": "dsp-cat-in-dataset",
    "type": "http in",
    "z": "tab-dsp-catalogue",
    "name": "GET /dsp/catalog/datasets/:id",
    "url": "/dsp/catalog/datasets/:id",
    "method": "get",
    "upload": false,
    "swaggerDoc": "",
    "x": 190,
    "y": 340,
    "wires": [
      [
        "dsp-cat-dataset-fn"
      ]
    ]
  },
  {
    "id": "dsp-cat-dataset-fn",
    "type": "function",
    "z": "tab-dsp-catalogue",
    "name": "dataset lookup",
    "func": "function dspCatalogError(statusCode, code, reasons) {\n    msg.statusCode = statusCode;\n    msg.headers = { 'Content-Type': 'application/json' };\n    msg.payload = {\n        '@context': ['https://w3id.org/dspace/2025/1/context.jsonld'],\n        '@type': 'CatalogError',\n        code: code,\n        reason: reasons\n    };\n    return msg;\n}\n\nconst datasets = flow.get('catalogue') || [];\nconst id = decodeURIComponent(msg.req.params.id || '');\nconst found = datasets.find((d) => d && d.id === id);\nif (!found) {\n    return dspCatalogError(404, 'not_found', ['Dataset ' + id + ' not found']);\n}\nmsg.statusCode = 200;\nmsg.headers = { 'Content-Type': 'application/json' };\nconst md = (found && found.metadata) || {};\n    // NF-7 (audit 2026-07-24): DSP DatasetRequest returns a dcat:Dataset JSON-LD\n    // (with @context so the response JSON-LD-expands to @id/@type), not the raw record.\n    msg.payload = {\n        '@context': ['https://w3id.org/dspace/2025/1/context.jsonld'],\n        '@type': 'dcat:Dataset',\n        '@id': found.id,\n        'dct:title': md.title || found.id,\n        'dct:description': md.description || '',\n        'dcat:distribution': [ { '@type': 'dcat:Distribution', 'dct:format': md.format || 'application/json' } ],\n        'odrl:hasPolicy': (Array.isArray(found.offers) ? found.offers : []).map(function (o) {\n            return { '@type': 'odrl:Offer', '@id': (o && o.id) || (found.id + ':offer'), 'odrl:permission': [ { 'odrl:target': found.id, 'odrl:action': 'odrl:use' } ] };\n        })\n    };\nreturn msg;\n",
    "outputs": 1,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 410,
    "y": 340,
    "wires": [
      [
        "dsp-cat-response"
      ]
    ]
  },
  {
    "id": "dsp-cat-mode-check",
    "type": "function",
    "z": "tab-dsp-catalogue",
    "name": "catalogue mode gate",
    "func": "const mode = (env.get('DSP_IAM_CATALOGUE') || 'open').toLowerCase();\nif (mode === 'verified') {\n    return [null, msg];\n}\nreturn [msg, null];\n",
    "outputs": 2,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 280,
    "y": 80,
    "wires": [
      [
        "dsp-cat-query"
      ],
      [
        "dsp-cat-iam-call"
      ]
    ]
  },
  {
    "id": "dsp-cat-iam-call",
    "type": "link call",
    "z": "tab-dsp-catalogue",
    "name": "iam.verify",
    "links": [
      "dsp-iam-verify-in"
    ],
    "timeout": "5",
    "x": 420,
    "y": 120,
    "wires": [
      [
        "dsp-cat-iam-branch"
      ]
    ]
  },
  {
    "id": "dsp-cat-iam-branch",
    "type": "switch",
    "z": "tab-dsp-catalogue",
    "name": "iamRejected?",
    "property": "iamRejected",
    "propertyType": "msg",
    "rules": [
      {
        "t": "true"
      },
      {
        "t": "false"
      }
    ],
    "checkall": "true",
    "repair": false,
    "outputs": 2,
    "x": 480,
    "y": 160,
    "wires": [
      [
        "dsp-cat-response"
      ],
      [
        "dsp-cat-query"
      ]
    ]
  },
  {
    "id": "dsp-cat-query",
    "type": "function",
    "z": "tab-dsp-catalogue",
    "name": "query_catalogue",
    "func": "function dspCatalogError(statusCode, code, reasons) {\n    msg.statusCode = statusCode;\n    msg.headers = { 'Content-Type': 'application/json' };\n    msg.payload = {\n        '@context': ['https://w3id.org/dspace/2025/1/context.jsonld'],\n        '@type': 'CatalogError',\n        code: code,\n        reason: reasons\n    };\n    return msg;\n}\n\n// Gap 5 (NF-7): the DSP binding spelling POST /dsp/catalog/request answers with\n// a DSP 2025-1 dcat:Catalog of dcat:Dataset entries; the FACIS spelling\n// POST /dsp/catalogue/request keeps its native { datasets, nextCursor } shape.\nfunction toDcatDataset(d) {\n    const md = (d && d.metadata) || {};\n    const distribution = { '@type': 'dcat:Distribution', 'dct:format': md.format || 'application/json' };\n    const hasPolicy = (Array.isArray(d && d.offers) ? d.offers : []).map(function (o) {\n        return { '@type': 'odrl:Offer', '@id': (o && o.id) || (d.id + ':offer'), 'odrl:permission': [ { 'odrl:target': d.id, 'odrl:action': 'odrl:use' } ] };\n    });\n    return { '@type': 'dcat:Dataset', '@id': d.id, 'dct:title': md.title || d.id, 'dct:description': md.description || '', 'dcat:distribution': [ distribution ], 'odrl:hasPolicy': hasPolicy };\n}\nfunction toDcatCatalog(items, next) {\n    const cat = {\n        '@context': ['https://w3id.org/dspace/2025/1/context.jsonld'],\n        '@type': 'dcat:Catalog',\n        '@id': 'urn:facis:dsp:catalog',\n        'dct:title': 'FACIS Data Space Catalog',\n        'dspace:participantId': env.get('DSP_PARTICIPANT_ID') || 'urn:facis:dsp:provider',\n        'dcat:dataset': (items || []).map(toDcatDataset)\n    };\n    if (next) cat['dspace:nextCursor'] = next;\n    return cat;\n}\n\n// Mirrors catalogue_store.query_catalogue() filter + cursor logic.\nconst datasets = flow.get('catalogue') || [];\nconst body = (msg.payload && typeof msg.payload === 'object') ? msg.payload : {};\nif (body.filter !== undefined && (typeof body.filter !== 'object' || body.filter === null || Array.isArray(body.filter))) {\n    return [dspCatalogError(400, 'unsupported_filter', ['filter must be an object']), null];\n}\nconst filter = body.filter || {};\nconst page = body.page || {};\n\nconst limit = (typeof page.limit === 'number' && page.limit > 0) ? page.limit : 50;\nconst cursorRaw = page.cursor;\n\nlet filtered = datasets;\nif (filter.assetType) {\n    filtered = filtered.filter(d => d && d.metadata && d.metadata.assetType === filter.assetType);\n}\nif (Array.isArray(filter.datasetIds) && filter.datasetIds.length > 0) {\n    const ids = new Set(filter.datasetIds);\n    filtered = filtered.filter(d => d && ids.has(d.id));\n}\n\n// Python: int(cursor) on ValueError -> 0; missing/null -> 0.\nlet start = 0;\nif (cursorRaw !== null && cursorRaw !== undefined && cursorRaw !== '') {\n    const parsed = parseInt(cursorRaw, 10);\n    start = Number.isFinite(parsed) && parsed >= 0 ? parsed : 0;\n}\n\nconst slice = filtered.slice(start, start + limit);\nconst nextCursor = (start + limit) < filtered.length ? String(start + limit) : null;\n\nconst reqUrl = String((msg.req && (msg.req.originalUrl || msg.req.url)) || '');\nconst wantsDcat = reqUrl.indexOf('/catalog/request') !== -1 && reqUrl.indexOf('/catalogue/request') === -1;\nmsg.payload = wantsDcat ? toDcatCatalog(slice, nextCursor) : { datasets: slice, nextCursor: nextCursor };\nmsg.headers = { 'Content-Type': 'application/json' };\nmsg.statusCode = 200;\nreturn [msg, { payload: { family: 'facis_dsp_catalogue_requests_total' } }];\n",
    "outputs": 2,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 470,
    "y": 260,
    "wires": [
      [
        "dsp-cat-response"
      ],
      [
        "dsp-cat-link-out-metric"
      ]
    ]
  },
  {
    "id": "dsp-cat-response",
    "type": "http response",
    "z": "tab-dsp-catalogue",
    "name": "200 catalogue",
    "statusCode": "",
    "headers": {},
    "x": 720,
    "y": 260,
    "wires": []
  },
  {
    "id": "dsp-cat-link-out-metric",
    "type": "link out",
    "z": "tab-dsp-catalogue",
    "name": "dsp.metric.inc \u2192",
    "mode": "link",
    "links": [
      "dsp-link-in-metric-inc"
    ],
    "x": 720,
    "y": 320,
    "wires": []
  }
]