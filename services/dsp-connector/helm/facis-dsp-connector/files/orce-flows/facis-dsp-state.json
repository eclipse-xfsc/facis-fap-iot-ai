[
  {
    "id": "tab-dsp-state",
    "type": "tab",
    "label": "FACIS DSP — State Bootstrap & Persistence",
    "disabled": false,
    "info": "Postgres-backed persistence for DSP transfers and negotiations.\n\nStartup: dsp-state-restore-fn creates the dsp_transfers / dsp_negotiations\ntables if absent, then loads every row into GLOBAL context as\nglobal.set('transfers') / global.set('negotiations'). Both stores are\nglobal-scoped, not flow-scoped: the link-producing flows and the health\nendpoint live on other tabs, and Node-RED flow-context is tab-local.\n\nOne-time migration: on first boot against an empty table, the legacy\nPVC JSON (/data/dsp-state/{transfers,negotiations}.json) is imported once,\nthen Postgres is the sole source of truth.\n\nPersistence: mutating flows fire the dsp.persist.transfers /\ndsp.persist.negotiations links with an empty trigger; the receiver re-reads\nthe authoritative in-memory map and replaces the table contents in one\ntransaction (snapshot semantics, same as the old file store).\n\nRetry: if Postgres is still booting when this instance starts, the restore\nraises on its catch node, which rate-limits (1/15s, drop) a retry until the\ndatabase accepts connections. Persist failures node.error but never crash."
  },
  {
    "id": "dsp-state-startup",
    "type": "inject",
    "z": "tab-dsp-state",
    "name": "@startup",
    "props": [
      {
        "p": "payload"
      }
    ],
    "repeat": "",
    "crontab": "",
    "once": true,
    "onceDelay": "5",
    "topic": "",
    "payload": "",
    "payloadType": "str",
    "x": 130,
    "y": 80,
    "wires": [
      [
        "dsp-state-restore-fn"
      ]
    ]
  },
  {
    "id": "dsp-state-restore-fn",
    "type": "function",
    "z": "tab-dsp-state",
    "name": "restore state from PostgreSQL",
    "func": "// Restore DSP state from PostgreSQL into GLOBAL context (both stores are\n// global-scoped: link-producing flows and the health endpoint live on other\n// tabs, and Node-RED flow-context is tab-local).\n// One-time migration: if a table is empty and the legacy JSON file exists,\n// import it, then continue serving from Postgres.\n// ponytail: one pg.Client per operation — state writes are <= a few per\n// minute; switch to a pooled client only if that ever changes.\nconst DDL = [\n    `CREATE TABLE IF NOT EXISTS dsp_transfers (\n        id TEXT PRIMARY KEY,\n        state TEXT NOT NULL DEFAULT '',\n        doc JSONB NOT NULL,\n        updated_at TIMESTAMPTZ NOT NULL DEFAULT now())`,\n    `CREATE TABLE IF NOT EXISTS dsp_negotiations (\n        id TEXT PRIMARY KEY,\n        state TEXT NOT NULL DEFAULT '',\n        doc JSONB NOT NULL,\n        updated_at TIMESTAMPTZ NOT NULL DEFAULT now())`\n];\n\nconst LEGACY = {\n    dsp_transfers: '/data/dsp-state/transfers.json',\n    dsp_negotiations: '/data/dsp-state/negotiations.json'\n};\n\n(async () => {\n    const client = new pg.Client({ connectionString: env.get('DSP_PG_URI') });\n    await client.connect();\n    try {\n        for (const ddl of DDL) await client.query(ddl);\n\n        const maps = {};\n        for (const [table, legacyPath] of Object.entries(LEGACY)) {\n            const count = await client.query(`SELECT count(*)::int AS n FROM ${table}`);\n            if (count.rows[0].n === 0) {\n                let legacy = {};\n                try { legacy = JSON.parse(fs.readFileSync(legacyPath, 'utf8')); } catch (e) { /* no legacy file */ }\n                for (const [id, doc] of Object.entries(legacy)) {\n                    await client.query(\n                        `INSERT INTO ${table} (id, state, doc) VALUES ($1, $2, $3) ON CONFLICT (id) DO NOTHING`,\n                        [id, (doc && doc.state) || '', doc]);\n                }\n                if (Object.keys(legacy).length) {\n                    node.log('DSP state: migrated ' + Object.keys(legacy).length + ' row(s) from ' + legacyPath);\n                }\n            }\n            const res = await client.query(`SELECT id, doc FROM ${table}`);\n            const map = {};\n            res.rows.forEach((r) => { map[r.id] = r.doc; });\n            maps[table] = map;\n        }\n        global.set('transfers', maps.dsp_transfers);\n        global.set('negotiations', maps.dsp_negotiations);\n        // Gate for the persist fns: snapshot writes before restore has\n        // loaded the maps would truncate the tables with an empty map.\n        global.set('stateReady', true);\n        node.log('DSP state: ' + Object.keys(maps.dsp_transfers).length + ' transfer(s), '\n            + Object.keys(maps.dsp_negotiations).length + ' negotiation(s) restored from PostgreSQL');\n    } finally {\n        await client.end();\n    }\n})().catch((err) => {\n    // Surfaces on the tab's catch node, which rate-limits a retry — covers\n    // Postgres still booting when this instance starts.\n    node.error('DSP state restore failed: ' + (err && err.message ? err.message : err), msg);\n});\nreturn null;\n",
    "outputs": 0,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [
      {
        "var": "pg",
        "module": "pg"
      },
      {
        "var": "fs",
        "module": "fs"
      }
    ],
    "x": 420,
    "y": 80,
    "wires": []
  },
  {
    "id": "dsp-state-restore-catch",
    "type": "catch",
    "z": "tab-dsp-state",
    "name": "restore failed (Postgres booting?)",
    "scope": [
      "dsp-state-restore-fn"
    ],
    "uncaught": false,
    "x": 180,
    "y": 160,
    "wires": [
      [
        "dsp-state-retry-limit"
      ]
    ]
  },
  {
    "id": "dsp-state-retry-limit",
    "type": "delay",
    "z": "tab-dsp-state",
    "name": "retry at most every 15s",
    "pauseType": "rate",
    "timeout": "5",
    "timeoutUnits": "seconds",
    "rate": "1",
    "nbRateUnits": "15",
    "rateUnits": "second",
    "randomFirst": "1",
    "randomLast": "5",
    "randomUnits": "seconds",
    "drop": true,
    "allowrate": false,
    "outputs": 1,
    "x": 470,
    "y": 160,
    "wires": [
      [
        "dsp-state-restore-fn"
      ]
    ]
  },
  {
    "id": "dsp-link-in-persist-transfers",
    "type": "link in",
    "z": "tab-dsp-state",
    "name": "← dsp.persist.transfers",
    "links": [
      "dsp-tx-link-out-persist"
    ],
    "x": 150,
    "y": 300,
    "wires": [
      [
        "dsp-persist-transfers-fn"
      ]
    ]
  },
  {
    "id": "dsp-persist-transfers-fn",
    "type": "function",
    "z": "tab-dsp-state",
    "name": "persist transfers → PostgreSQL",
    "func": "// Refuse to snapshot before the boot restore has populated the maps —\n// an early persist would truncate the table with an empty map.\nif (!global.get('stateReady')) {\n    node.warn('DSP state persist skipped: restore has not completed yet');\n    return null;\n}\n// Snapshot semantics preserved from the file-based store: re-read the\n// authoritative in-memory map and replace the table contents atomically.\n// ponytail: one pg.Client per operation — state writes are <= a few per\n// minute; switch to a pooled client only if that ever changes.\n(async () => {\n    const transfers = global.get('transfers') || {};\n    const client = new pg.Client({ connectionString: env.get('DSP_PG_URI') });\n    await client.connect();\n    try {\n        await client.query('BEGIN');\n        await client.query('DELETE FROM dsp_transfers');\n        for (const [id, doc] of Object.entries(transfers)) {\n            await client.query(\n                'INSERT INTO dsp_transfers (id, state, doc) VALUES ($1, $2, $3)',\n                [id, (doc && doc.state) || '', doc]);\n        }\n        await client.query('COMMIT');\n    } catch (err) {\n        try { await client.query('ROLLBACK'); } catch (e) { /* connection gone */ }\n        throw err;\n    } finally {\n        await client.end();\n    }\n})().catch((err) => {\n    node.error('DSP state persist (transfers) failed: ' + (err && err.message ? err.message : err), msg);\n});\nreturn null;\n",
    "outputs": 0,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [
      {
        "var": "pg",
        "module": "pg"
      }
    ],
    "x": 430,
    "y": 300,
    "wires": []
  },
  {
    "id": "dsp-link-in-persist-negotiations",
    "type": "link in",
    "z": "tab-dsp-state",
    "name": "← dsp.persist.negotiations",
    "links": [
      "dsp-neg-link-out-persist"
    ],
    "x": 150,
    "y": 380,
    "wires": [
      [
        "dsp-persist-negotiations-fn"
      ]
    ]
  },
  {
    "id": "dsp-persist-negotiations-fn",
    "type": "function",
    "z": "tab-dsp-state",
    "name": "persist negotiations → PostgreSQL",
    "func": "// Refuse to snapshot before the boot restore has populated the maps —\n// an early persist would truncate the table with an empty map.\nif (!global.get('stateReady')) {\n    node.warn('DSP state persist skipped: restore has not completed yet');\n    return null;\n}\n// Snapshot semantics preserved from the file-based store: re-read the\n// authoritative in-memory map and replace the table contents atomically.\n// ponytail: one pg.Client per operation — state writes are <= a few per\n// minute; switch to a pooled client only if that ever changes.\n(async () => {\n    const negotiations = global.get('negotiations') || {};\n    const client = new pg.Client({ connectionString: env.get('DSP_PG_URI') });\n    await client.connect();\n    try {\n        await client.query('BEGIN');\n        await client.query('DELETE FROM dsp_negotiations');\n        for (const [id, doc] of Object.entries(negotiations)) {\n            await client.query(\n                'INSERT INTO dsp_negotiations (id, state, doc) VALUES ($1, $2, $3)',\n                [id, (doc && doc.state) || '', doc]);\n        }\n        await client.query('COMMIT');\n    } catch (err) {\n        try { await client.query('ROLLBACK'); } catch (e) { /* connection gone */ }\n        throw err;\n    } finally {\n        await client.end();\n    }\n})().catch((err) => {\n    node.error('DSP state persist (negotiations) failed: ' + (err && err.message ? err.message : err), msg);\n});\nreturn null;\n",
    "outputs": 0,
    "timeout": "",
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [
      {
        "var": "pg",
        "module": "pg"
      }
    ],
    "x": 440,
    "y": 380,
    "wires": []
  }
]
